# Test Data

This directory contains fixtures used by tests and benchmarks.

## Layout

- `fixtures/`: Generated TL binary fixtures (created by scripts)

## Generating fixtures

Run `scripts/setup-testdata.sh` to regenerate fixtures.
